export enum Category {
    HandWatches = "hand_watches",
    WallClocks = "wall_clocks",
}

export enum Status {
    Available = "Available",
    Sold = "Sold",
    Returned = "Returned",
    Damaged = "Damaged",
}